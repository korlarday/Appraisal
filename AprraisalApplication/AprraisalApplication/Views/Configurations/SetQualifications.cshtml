@model AprraisalApplication.Models.ViewModels.SetQualificationsVM

@{
    ViewBag.Title = "Set Qualifications";
}


<!-- Hero -->
<div class="bg-body-light">
    <div class="content content-full">
        <div class="d-flex flex-column flex-sm-row justify-content-sm-between align-items-sm-center">
            <h1 class="flex-sm-fill font-size-h2 font-w400 mt-2 mb-0 mb-sm-2">Set Qualifications</h1>
            <nav class="flex-sm-00-auto ml-sm-3" aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item">Configurations</li>
                    <li class="breadcrumb-item">Resourses</li>
                    <li class="breadcrumb-item active" aria-current="page">Set Qualifications</li>
                </ol>
            </nav>
        </div>
    </div>
</div>
<!-- END Hero -->
<!-- Page Content -->
<div class="content">
    <!-- Elements -->
    <div class="block block-rounded block-bordered">
        <div class="block-header block-header-default">
            <h3 class="block-title">Set Qualifications</h3>
        </div>
        <div class="block-content">
            <div class="block block-rounded block-bordered">
                <div class="block-content block-content-full">
                    <div>
                        <button type="button" class="btn btn-outline-primary push" data-toggle="modal" data-target="#new_qualification_modal"><i class="fa fa-plus-circle"></i> Add New Qualification</button>
                        <button type="button" style="display:none" data-toggle="modal" id="show_edit_modal" data-target="#edit_qualification_modal">Edit Qualification</button>
                    </div>
                    <!-- DataTables init on table by adding .js-dataTable-buttons class, functionality is initialized in js/pages/be_tables_datatables.min.js which was auto compiled from _es6/pages/be_tables_datatables.js -->
                    <div class="table-responsive">
                        <table class="table table-bordered table-striped table-vcenter js-dataTable-full-pagination" data-page-length='10' id="table">
                            <thead>
                                <tr>
                                    <th style="display:none">Id</th>
                                    <th class="text-center">#</th>
                                    <th>State</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody id="table_body">
                                @{
                                    int num = 1;
                                    foreach (var item in Model.Qualifications)
                                    {
                                        <tr id="@item.Id">
                                            <td class="id" style="display: none">@item.Id</td>
                                            <td class="text-center count-item">@num</td>
                                            <td class="qualificationname">
                                                @item.Name
                                            </td>
                                            <td class="text-center">
                                                <div class="btn-group">
                                                    <a href="#" class="btn btn-sm btn-outline-primary edit_qualification" data-toggle="tooltip" title="Edit">
                                                        <i class="fa fa-pencil-alt"></i>
                                                    </a>
                                                    <a href="#" class="btn btn-sm btn-outline-danger delete_qualification" data-toggle="tooltip" title="Delete">
                                                        <i class="fa fa-trash"></i>
                                                    </a>
                                                </div>
                                            </td>
                                        </tr>
                                        ++num;
                                    }
                                }

                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- END Elements -->
</div>
<!-- END Page Content -->
<!-- New Subject Modal -->
<div class="modal fade" id="new_qualification_modal" tabindex="-1" role="dialog" aria-labelledby="new_qualification_modal" aria-hidden="true">
    <div class="modal-dialog modal-dialog-popout" role="document">
        <div class="modal-content">
            <div class="block block-themed block-transparent mb-0">
                <div class="block-header bg-primary-dark">
                    <h3 class="block-title">Create New Qualification</h3>
                    <div class="block-options">
                        <button type="button" class="btn-block-option" data-dismiss="modal" aria-label="Close">
                            <i class="fa fa-fw fa-times"></i>
                        </button>
                    </div>
                </div>
                <div class="block-content">
                    <div class="row push">
                        <div class="col-md-8 offset-md-2">
                            <div class="form-group">
                                @Html.LabelFor(model => model.CreateQualification.Name)
                                @Html.EditorFor(model => model.CreateQualification.Name, new { htmlAttributes = new { @class = "form-control", id = "qualification_name" } })
                            </div>

                            <div class="form-group">
                                <button type="submit" id="submit_qualification" class="btn btn-primary mt-3">Create Qualification</button>
                            </div>

                        </div>
                    </div>
                </div>
                <div class="block-content block-content-full text-right bg-light">
                    <button type="button" class="btn btn-sm btn-light close_modal" data-dismiss="modal">Close</button>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- END  -->
<!-- Edit Subject Modal -->
<div class="modal fade" id="edit_qualification_modal" tabindex="-1" role="dialog" aria-labelledby="edit_qualification_modal" aria-hidden="true">
    <div class="modal-dialog modal-dialog-popout" role="document">
        <div class="modal-content">
            <div class="block block-themed block-transparent mb-0">
                <div class="block-header bg-primary-dark">
                    <h3 class="block-title">Edit Qualification</h3>
                    <div class="block-options">
                        <button type="button" class="btn-block-option" data-dismiss="modal" aria-label="Close">
                            <i class="fa fa-fw fa-times"></i>
                        </button>
                    </div>
                </div>
                <div class="block-content">
                    <div class="row push">
                        <div class="col-md-8 offset-md-2">
                            <input type="hidden" id="edit_id" />
                            <div class="form-group">
                                @Html.LabelFor(model => model.CreateQualification.Name)
                                @Html.EditorFor(model => model.CreateQualification.Name, new { htmlAttributes = new { @class = "form-control", id = "edit_qualification_name" } })
                            </div>

                            <div class="form-group">
                                <button type="button" id="update_qualification" class="btn btn-primary mt-3">Update Qualification</button>
                            </div>

                        </div>
                    </div>
                </div>
                <div class="block-content block-content-full text-right bg-light">
                    <button type="button" class="btn btn-sm btn-light close_modal" data-dismiss="modal">Close</button>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- END  -->

@section Scripts {

    <script>
        $(function () {

            // NEW Subject START
            var submitGradeButton = $("#submit_qualification");
            var qualificationName = $("#qualification_name");
            var table = $("#table");


            // SUBMITTING NEW Subject
            submitGradeButton.click(function (e) {
                e.preventDefault();
                var $this = $(this);
                $this.attr("disabled", true);
                var $qualificationNameText = qualificationName.val();
                if ($qualificationNameText.length < 1) {
                    Swal.fire("Oops...", "The qualification name field is required!", "error");
                    $this.attr("disabled", false);
                    return false;
                }
                var url = '@Url.Action("PostAddQualification", "Resources", new { httproute = "DefaultApi" })';
                $.ajax({
                    url: url,
                    data: {
                        qualificationName: $qualificationNameText
                    },
                    method: 'POST',
                    success: function (data) {
                        if (data.feedback == "taken") {
                            Swal.fire("Oops...", "The Qualification name exists!", "error");
                            $this.attr("disabled", false);
                            return false;
                        }
                        else if (data.feedback == 'required') {
                            Swal.fire("Oops...", "All fields are required!", "error");
                            $this.attr("disabled", false);
                            return false;
                        }
                        else {
                            if (!$("table#table").length) {
                                location.reload();
                            } else {
                                $this.attr("disabled", false);
                                $(".close_modal").click();
                                Swal.fire("Success...", "Qualification created successfully", "success");

                                qualificationName.val("");
                                var toAppend = $("table#table tbody tr:last").clone();
                                toAppend.attr("id", data.id);
                                toAppend.children(".id").text(data.id);
                                toAppend.children(".count-item").text(data.index);
                                toAppend.children(".qualificationname").text(data.qualificationName);

                                table.append($(toAppend).hide().fadeIn(1200));

                            }
                        }
                    },
                    error: function () {
                        console.log('Something went wrong');
                    }
                });
            })
            // END

            // For show edit
            $("#table").on("click", ".edit_qualification", function (e) {
                e.preventDefault();

                $this = $(this);
                var id = $this.closest('tr').children(".id").text();
                var qualificationname = $this.closest('tr').children(".qualificationname").text();

                $("#edit_qualification_name").val(qualificationname.trim());
                $("#edit_id").val(id);


                $("#show_edit_modal").click();
            });
            // END

            // submit edit
            var submitEdit = $("#update_qualification");
            submitEdit.click(function (e) {
                e.preventDefault();
                $this = $(this);
                $this.attr("disabled", true);

                var editId = $("#edit_id").val();
                var qualificationName = $("#edit_qualification_name").val();

                if (qualificationName.length < 1) {
                    Swal.fire("Oops...", "All fields are required!", "error");
                    $this.attr("disabled", false);
                    return false;
                }

                //var url = "/api/Resources/PostEditQualification";
                var url = '@Url.Action("PostEditQualification", "Resources", new { httproute = "DefaultApi" })';

                $.post(url,
                    { id: editId, qualificationName: qualificationName },
                    function (data) {
                        console.log(data);

                        if (data.feedback == "taken") {
                            Swal.fire("Oops...", "The qualification name is exists!", "error");
                            $this.attr("disabled", false);
                            return false;
                        }
                        else if (data.feedback == 'required') {
                            Swal.fire("Oops...", "All fields are required!", "error");
                            $this.attr("disabled", false);
                            return false;
                        }
                        else {
                            $this.attr("disabled", false);
                            $(".close_modal").click();
                            Swal.fire("Success...", "Qualification updated successfully", "success");

                            $("#edit_grade_name").val('')


                            $("#table_body").children("#" + data.id).hide().fadeIn(1000);
                            $("#table_body").children("#" + data.id).children(".qualificationname").text(data.qualificationName);

                        }
                    });
            })
            //END

            /*
             * Delete Subject js
             */
            $("#table").on("click", ".delete_qualification", function (e) {
                var $this = $(this);
                e.preventDefault();
                Swal.fire({
                    title: "Are you sure?",
                    text: "You won't be able to rever this!",
                    type: "warning",
                    showCancelButton: !0,
                    customClass: { confirmButton: "btn btn-danger m-1", cancelButton: "btn btn-secondary m-1" },
                    confirmButtonText: "Yes, delete it!",
                    html: !1,
                    preConfirm: function (e) { return new Promise(function (e) { setTimeout(function () { e() }, 50) }) }

                }).then(function (n) {
                    if (n.value) {
                        Swal.fire("Deleted!", "Qualification has been deleted.", "success");
                        var id = $this.closest('tr').children(".id").text();
                        //var url = "/api/Resources/PostDeleteQualification";
                        var url = '@Url.Action("PostDeleteQualification", "Resources", new { httproute = "DefaultApi" })';

                        $.post(url, { id: id }, function (data) {
                                $this.closest('tr').fadeOut("slow", function () {
                                    $(this).remove();
                                })
                        })
                    }
                    else {
                        "cancel" === n.dismiss && Swal.fire("Cancelled", "Cancelled", "error")
                    }

                })

            })
        })
                    ////////////////////////////////////////////////////////////////////
    </script>
}

